let pageTitle = document.getElementById("page-title");
let pageTitleContent = ""

if (pageTitle && !pageTitleContent){    
        pageTitleContent ="IPL 2025";
        pageTitle.innerText = pageTitleContent;   
}
